type Query {
  getAllBooks: [Book]!
  getBook(id: ID!): Book!
  getAllTodus: [Todus]!
  getTodu(id: ID!): Todus!
  # getAllAuthor: [Author]!
  # getAuthor(id: ID!): Author!
}
type Mutation {
  addBook(book: BookInput!): Book!
  updateBoock(book: UpdateBookInput!): Book!
  # addAuthor(book: AuthorInput!): Author!
  # updateBoock(book: UpdateAuthorInput!): Author!
  removeBoock(id: ID!): String!
  addTodu(todu: ToduInput!): Todus!
}
