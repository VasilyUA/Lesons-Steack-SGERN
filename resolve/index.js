const {
  getAllBooks,
  addBook,
  getBook,
  updateBoock,
  removeBoock,
} = require("./Boock");

const root = {
  getAllBooks,
  addBook,
  getBook,
  updateBoock,
  removeBoock,
};
module.exports = root;
